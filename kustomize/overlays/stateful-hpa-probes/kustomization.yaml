apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../stateful-hpa    # overlay 2 – StatefulSet + HPA

patches:
  - path: frontend-probes-patch.yaml
    target:
      kind: Deployment
      name: frontend
